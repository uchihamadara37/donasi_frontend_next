runtime: nodejs20

instance_class: donasiApp

env_variables:
  NEXT_PUBLIC_URL_SERVER: '${_NEXT_PUBLIC_URL_SERVER}'

automatic_scaling:
  min_instances: 0
  max_instances: 1

handlers:
  - url: /.*
    script: auto
